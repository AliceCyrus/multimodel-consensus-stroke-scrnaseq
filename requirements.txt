# ============================================================================
# Python dependencies
# ============================================================================
# These are the EXACT versions present in the environment used to produce the
# results in the paper (Python 3.7.9 on Windows 10).
#
#   python -m venv .venv
#   .venv\Scripts\activate         # Windows
#   source .venv/bin/activate      # macOS / Linux
#   pip install -r requirements.txt
#
# NOTE ON PYTHON VERSION
# Python 3.7 reached end of life in June 2023. The pins below are kept because
# they are what was actually run; see requirements-modern.txt for a Python
# 3.10+ set that reproduces the analysis with current releases. Numerical
# results are not bit-identical across these two sets, because scikit-learn
# and XGBoost changed internal defaults between the versions.
# ============================================================================

numpy==1.21.6
pandas==1.3.5
scipy==1.7.3
scikit-learn==1.0.2
xgboost==1.6.2
torch==1.13.1
tensorflow==1.15.0
shap==0.42.1
mlxtend==0.23.1
matplotlib==3.5.3
seaborn==0.12.2

# Mouse -> human ortholog mapping (map_mouse_to_human_orthologs.py only).
# Deliberately unpinned: this package is imported by that one optional script
# and was not present in the recorded environment, so no version can be
# asserted. It queries the live MyGene.info web service, which means its
# output depends on the date of the query rather than on the pinned version.
mygene
